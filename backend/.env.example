DATABASE_URL="file:./dev.db"
PORT=8080
# Leave blank in dev to skip API key checks. Set a long random string in production.
API_KEY=
# Replace with the ID that your browser generates.
EXTENSION_ID=
# Docker only: host path where DB and Caddyfile are stored. Defaults to ./data relative to docker-compose.yml.
DATA_DIR=
